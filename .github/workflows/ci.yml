# Stub CI workflow — replace with real PHP lint / WP tests when ready.
name: ci
on:
  push:
  pull_request:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Add PHP lint / plugin tests here"
